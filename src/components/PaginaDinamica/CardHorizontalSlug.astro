---
import type { ProyectChallenge } from "@/models";

type Props = {
  challenge: ProyectChallenge;
};

const { challenge } = Astro.props;

---

<article
  class="shadow-primary w-[80vw] max-w-[1000px] rounded-lg p-4 md:py-5 md:px-12 bg-white flex flex-col justify-between gap-5 min-h[300px]"
>
  <p class="text-sm lg:text-base font-medium">
    <span class="font-semibold">{challenge.name}</span> {challenge.description}
  </p>
  <p class="text-sm lg:text-base font-medium">
    <span class="font-semibold text-primary">Solución:</span> {challenge.solution}
  </p>
</article>

