---
title: "Estructuras condicionales"
description: "Condicionales en JavaScript"
---
import NumeroSecreto from "./SecretGame.jsx";

Al programar, uno de los conceptos fundamentales a dominar, son las estructuras condicionales, también conocidas como estructuras de control. Estas estructuas nos permiten decidir que parte de nuesrto código se ejecutará basado en ciertas condiciones.

## if, else y else if

La más básica de estas es **IF**, esta nos permite ejecutar un bloque de código solo si la condición es verdadera.

### Estructura:

```js
if (condición) {
  //código a ejecutar
} else {
  //Solo se ejecuta si las anteriores validaciones no son verdaderas
}
```

### Ejemplo:

```js
let edad = 18;
if (edad >= 18) {
  console.log("Eres mayor de edad");
} else {
  console.log("Eres menor de edad");
}
```
## Juego del número secreto
<NumeroSecreto client:load />

## Switch

La condición switch en JavaScript es una estructura de control que permite ejecutar diferentes bloques de código según si el valor de una expresión es true.

Es especialmente útil cuando necesitas manejar múltiples casos basados en el valor de una variable, proporcionando una alternativa más limpia y legible a múltiples declaraciones.

### Estrucctura

```js
switch (expresión) {
  case valor1:
    //código a ejecutar
    break;
  case valor2:
    //código a ejecutar
    break;
  default:
    //código a si ninguno de los anteriores fue true
    break;
}
```

### Ejemplo

```js
let fruta = "Papaya";

switch (fruta) {
  case "Manzana":
    console.log("Las manzanas cuestan 40mil el kilo");
    break;
  case "Papaya":
    console.log("las papayas cuestan 15mil el kilo");
    break;
  case "Mangos": //Se pueden concatenar respuestas
  case "Sandia":
    console.log("Cada uno cuesta 25mil el kilo");
    break;
  default:
    console.log(`lo siento, no tenemos ${fruta}`);
    break;
}
```


